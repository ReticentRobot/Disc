<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Disc.Views.HomePage" Title="All"
             xmlns:local="clr-namespace:Disc"
             xmlns:vm="clr-namespace:Disc.ViewModels"
             x:DataType="vm:PostsViewModel"
             xmlns:converters="clr-namespace:Disc.Converters;assembly=Disc"
             xmlns:mct="clr-namespace:CommunityToolkit.Maui.Behaviors;assembly=CommunityToolkit.Maui"
             >
    <!--<ContentPage.Behaviors>
        <mct:EventToCommandBehavior Command="{Binding AppearingCommand}" EventName="Appearing" />
        <mct:EventToCommandBehavior Command="{Binding DisappearingCommand}" EventName="Disappearing" />
    </ContentPage.Behaviors>-->

    <CollectionView 
        ItemsSource="{Binding Posts}"
        BackgroundColor="{AppThemeBinding Light=Pink, Dark=#1C1D20}"
        RemainingItemsThreshold="5"
        RemainingItemsThresholdReached="OnCollectionViewRemainingItemsThresholdReached">
        <!--RemainingItemsThresholdReachedCommand="{Binding IsLoadingMoreItems}"-->
        
        <!--<CollectionView.ItemsLayout>
            <LinearItemsLayout Orientation="Vertical" ItemSpacing="10"/>
        </CollectionView.ItemsLayout>-->
        <CollectionView.ItemTemplate>
            <DataTemplate>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width=".5*" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <Border 
                        Stroke="#1C1D20" 
                        StrokeThickness="0" 
                        StrokeShape="RoundRectangle 15"  
                        Grid.RowSpan="5"
                        Margin="10"
                        Padding="10"
                        HeightRequest="75"
                        WidthRequest="75"
                        >
                        <Image 
                            Grid.RowSpan="5"
                            Source="{Binding Link.Image.ImageUrl, StringFormat='https://discuit.net{0}'}"
                            Aspect="AspectFill"
                            HeightRequest="80"
                            WidthRequest="80" />
                    </Border>
                    <Label 
                        Grid.Column="1"
                        Grid.Row="1"
                        Text="{Binding Title}"
                        LineBreakMode="WordWrap"
                        MaxLines="2"
                        FontAttributes="Bold" 
                        Padding="0,10,5,0"
                        />
                    <Label 
                        x:Name="communityAndUserLabel"
                        Grid.Row="2"
                        Grid.Column="1"
                        FontAttributes="None"
                        VerticalOptions="End">
                        <Label.FormattedText>
                            <FormattedString>
                                <Span Text="{Binding CommunityName, Mode=TwoWay}" TextColor="{Binding CommunityName, Converter={StaticResource nameColorMulti}, ConverterParameter=community}" />
                                <Span Text=" • " TextColor="Grey" />
                                <Span Text="{Binding Username, Mode=TwoWay}" TextColor="{Binding Username, Converter={StaticResource nameColorMulti}, ConverterParameter=username}" />
                            </FormattedString>
                        </Label.FormattedText>
                    </Label>
                        <!--<Label 
                        x:Name="communityAndUserLabel"
                        Grid.Row="2"
                        Grid.Column="1"
                        FontAttributes="None"
                        VerticalOptions="End">
                        <Label.Text>
                            <MultiBinding StringFormat="{}{0} • {1}" Mode="TwoWay">
                                <Binding Path="CommunityName" />
                                <Binding Path="Username" />
                            </MultiBinding>
                        </Label.Text>
                        <Label.TextColor>
                            <MultiBinding Converter="{StaticResource nameColorMulti}" ConverterParameter="community">
                                <Binding Path="CommunityName" />
                                <Binding Path="Username" />
                            </MultiBinding>
                        </Label.TextColor>
                    </Label>-->
                    <Label
                        Grid.Row="3"
                        Grid.Column="1"
                        TextColor="Grey"
                        FontAttributes="None">
                        <MultiBinding StringFormat="{}{0:M/d/y - ddd h:mmt} • {1}">
                            <Binding Path="CreatedAt" />
                            <Binding Path="Link.Hostname" />
                        </MultiBinding>
                    </Label>
                    <Label
                        Grid.Row="4"
                        Grid.Column="1"
                        TextColor="Grey"
                        FontAttributes="None"
                        VerticalOptions="End" 
                        Padding="0,0,0,10">
                        <Label.Text>
                            <MultiBinding StringFormat="{}{0} Upvotes • {1} Downvotes • {2} Comments">
                                <Binding Path="Upvotes" />
                                <Binding Path="Downvotes" />
                                <Binding Path="NoComments" />
                            </MultiBinding>
                        </Label.Text>
                    </Label>
                    <BoxView
                        Grid.Row="0"
                        Grid.ColumnSpan="2"
                        HeightRequest="3" 
                        Color="Black" 
                        HorizontalOptions="Fill"
                        VerticalOptions="Fill"/>
                </Grid>
            </DataTemplate>
        </CollectionView.ItemTemplate>
    </CollectionView>
</ContentPage>